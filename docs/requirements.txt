# Sphinx and documentation-specific dependencies only
# Project dependencies are installed automatically from pyproject.toml
# when the package is installed via "path: ." in .readthedocs.yaml
sphinx>=7.0
sphinx-autodoc-typehints>=1.24
furo>=2023.9.10
myst-parser>=2.0
nbsphinx>=0.9
sphinx-copybutton>=0.5 